{{>partial_header Header}}
#nullable enable
#nullable disable warnings
#pragma warning disable

namespace {{PackageName}}
{ {{#if Operations}}
    public static class {{ClassName}}
    {
        {{#each Operations as |Operation|}}{{#if Operation.Summary}}
        /// <summary>
        /// {{linewrap Operation.Summary "
        /// "}}
        /// </summary>{{/if}}{{#if Operation.Description}}
        /// <remarks>
        /// {{linewrap Operation.Description "
        /// "}}
        /// </remarks>{{/if}}
        public static global::Microsoft.AspNetCore.Builder.IEndpointConventionBuilder Map{{{Operation.Name}}}<T>(
            this global::Microsoft.AspNetCore.Routing.IEndpointRouteBuilder endpoints,
            global::System.Threading.CancellationToken cancellationToken = default(global::System.Threading.CancellationToken)
        ) where T : {{{Operation.AbstractTypeName}}} => endpoints.Map("{{{Operation.Path}}}", () => {
            // TODO
            // Activator.Create
            // Determine which Request Body
            // Parse Request
            // Translate Response
        });
        {{/each}}
    }
{{/if}}
}